#include<bits/stdc++.h>
using namespace std;
int n,m;

int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);cout.tie(0);
	cin >> n >> m;
	int u,v;
	for(int i=1;i<=m;i++)	cin >> u >> v;
	int q;	cin >> q;
	while(q--)	cout << "NO\n";
	
	return 0;
}
